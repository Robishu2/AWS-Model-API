version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
  build:
    commands:
      - echo "Creating deployment package..."
      - zip deployment_package.zip lambda_function.py

artifacts:
  files:
    - deployment_package.zip